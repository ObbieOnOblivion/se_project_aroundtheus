import Popup from "./popup";
class popupWithImage extends Popup{
    constructor(popupSelector){
        super(popupSelector) // might not need this
    };

    close(){
        super.close();
    }

};
